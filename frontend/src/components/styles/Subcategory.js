import React from "react"
import { Heading } from "rebass"

const Subcategory = props => (
  <Heading
    color="black"
    fontSize={[2]}
    mx={[4]}
    my={[2]}
    fontWeight="100"
    {...props}
  />
)

export default Subcategory
